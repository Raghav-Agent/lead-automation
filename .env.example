# Environment variables for lead automation
# AI (choose one provider in config.yaml):
# - openclaw (integrated): set ai.provider=openclaw, set OPENROUTER_API_KEY here, and run openclaw-ai service
# - ollama (local): set ai.provider=ollama, ensure Ollama running, optionally set OLLAMA_BASE_URL
# - groq (free tier): set ai.provider=groq, set GROQ_API_KEY here
# - openai (paid): set ai.provider=openai, set OPENAI_API_KEY here
OPENROUTER_API_KEY=your-openrouter-key  # used by openclaw, groq, openai
OPENAI_API_KEY=your-openai-key
GROQ_API_KEY=your-groq-key

# Email sending (Gmail example):
SMTP_PASSWORD=your-gmail-app-password
# Or use SendGrid free tier:
# SENDGRID_API_KEY=your-sendgrid-key

# IMAP for reply monitoring (optional): Gmail IMAP settings
IMAP_SERVER=imap.gmail.com
IMAP_USER=you@example.com
IMAP_PASSWORD=your-gmail-app-password

# Places search (free options):
# Yelp Fusion (free 5000/day): set places.provider=yelp in config.yaml and set YELP_API_KEY here
YELP_API_KEY=your-yelp-api-key
# OpenStreetMap Nominatim (free, rate-limited): set places.provider=osm in config.yaml (no key needed)

# Note: No API keys needed for OSM or pattern-based email enrichment.